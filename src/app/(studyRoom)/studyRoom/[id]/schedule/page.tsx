'use client';
import MainLayout from '@/components/studyRoom/MainLayout'

const page = () => {
  return (
    <div>
      <MainLayout list={[]} selectedItem={''} setSelectedItem={() => {}}/>
    </div>
  )
}
export default page